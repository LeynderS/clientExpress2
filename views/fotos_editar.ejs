<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>
    <%= title %>
  </title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

</head>

<body class="bg-light">
  <div class="container mt-5">
    <h1 class="text-center text-primary mb-4">
      <%= title %>
    </h1>

    <div class="row justify-content-center">
      <div class="col-md-6">
        <form action="/photos/update" method="POST">
          <div class="form-group mb-3">
            <label for="id_visible" class="form-label">ID No Editable</label>
            <input type="text" class="form-control" id="id_visible" value="<%= foto.id %>" readonly>
            <input type="hidden" name="id" value="<%= foto.id %>">
          </div>

          <div class="form-group mb-3">
            <label for="title" class="form-label">Título</label>
            <input type="text" class="form-control" id="title" name="title" value="<%= foto.titulo %>" required>
          </div>

          <div class="form-group mb-3">
            <label for="description" class="form-label">Descripción</label>
            <textarea class="form-control" id="description" name="description" rows="3"
              required><%= foto.descripcion %></textarea>
          </div>

          <div class="form-group mb-3">
            <label for="rate" class="form-label">Calificación (0.0 - 10.0)</label>
            <input type="number" class="form-control" id="rate" name="rate" min="0" max="10" step="0.1"
              value="<%= foto.calificacion %>" required>
          </div>

          <div class="form-group mt-2 text-center">
            <label for="route" class="form-label">Selecciona una imagen</label>
            <input type="file" name="route" id="route" class="form-control mt-2" accept=".png,.jpg,.jpeg" />
            <div class="form-text">* Si no seleccionas una imagen, se conservará la actual.</div>
          </div>

          <div class="text-center">
            <button type="submit" class="btn btn-success">Actualizar Foto</button>
            <a href="/photos" class="btn btn-secondary ms-2">Cancelar</a>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>